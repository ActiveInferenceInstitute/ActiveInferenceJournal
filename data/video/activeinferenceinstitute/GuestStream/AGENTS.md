# Agent guide — GuestStream/

Each subfolder is one **item** (event) in the GuestStream series, following the per-item schema ([`../../../../docs/SCHEMA.md`](../../../../docs/SCHEMA.md)). `metadata.json` is the source of truth (parts = video ids). Generated by Journal-Utilities — edit the generator, not derived files.
